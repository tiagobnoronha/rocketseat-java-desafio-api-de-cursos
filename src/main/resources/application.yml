spring:
    application:
        name: rocket-api-curso

    datasource:
        url: jdbc:postgresql://localhost:5432/api_cursos
        username: admin
        password: admin
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true     
